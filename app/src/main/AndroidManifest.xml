<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mx.monzon.masterdesarrolloandroidjavaykotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MasterDesarrolloAndroidJavaYKotlin"
        android:name=".MasterApplication">
        <activity android:name=".BottomNavNotasActivity"></activity>
        <activity android:name=".ButtonsActivity"></activity>
        <activity android:name=".RadioButtonActivity"></activity>
        <activity android:name=".CheckBoxActivity"></activity>
        <activity android:name=".ui.views.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>